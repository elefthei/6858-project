import socket, sys

s = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
s.connect("/tmp/chldsock-1")
while 1:
    sys.stdout.write('$ ')
    command=sys.stdin.readline()
    s.send(command)
    data = s.recv(4096)
    sys.stdout.write(data)

